-- UI Layer
-- Visual enhancements loaded after plugins

local M = {}

function M.setup()
    -- Visual enhancements can be added here
    -- Currently handled by plugin configs (colorscheme, lualine, etc.)
end

-- Auto-setup
M.setup()

return M
