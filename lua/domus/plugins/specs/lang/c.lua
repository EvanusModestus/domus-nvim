-- C Language Specs
-- clangd configured in lsp/init.lua, clang-format in conform.lua, codelldb in dap/init.lua
-- Snippets live in lua/domus/snippets/c.lua and are registered by the single
-- LuaSnip spec in specs/coding.lua (avoids a duplicate LuaSnip spec whose
-- config would override the friendly-snippets loader).

return {}
